# quantapact-action-test

Throwaway test repo for verifying `mzon7/quantapact/action@main` (Action v2: SARIF + lockfile generation).

Created automatically. Safe to delete after the workflow run completes.
